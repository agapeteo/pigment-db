# V2 WAL Diagnostic V3 Result

**Classification**: Environment/measurement remediation is supported; a candidate-specific CPU regression is not.

The capture is structurally valid: all 66 measured rows have the expected identities, affinity, operation counts, three store families, eleven counterbalanced pairs, positive timings, and complete worker/system accounting. The runner exited successfully.

## Capture evidence

| Field | Value |
|---|---|
| Capture ID | `diagnostic-v3-20260808-001649` |
| Launch | `2026-08-08T00:29:12-05:00` |
| Completion | `2026-08-08T00:35:16-05:00` |
| Baseline commit | `f5bf40e9861b544f867d5aa940fa52eb940b5e54` |
| Candidate source SHA-256 | `283e6c3920baa0e7a6bf370d17786a78033cf6fa98edf893bc67876d6b44388f` |
| Affinity | `12-19` |
| Raw rows | 66 measured rows plus one header |
| Raw CSV SHA-256 | `5f4ce2c5a34dda95bd74b5c7165a1ffc670b1da4eceef34315211200589f76c1` |
| Runner result | Exit code 0; no operation, accounting, or validation failures |

The raw capture is preserved byte-for-byte in `diagnostic-v3-20260808-001649.csv`.

## Worker-accounted results

| Cell | Baseline median writes/s | Candidate median writes/s | Wall ratio | Baseline median CPU ticks | Candidate median CPU ticks | CPU-tick ratio | Minimum pair ratio |
|---|---:|---:|---:|---:|---:|---:|---:|
| key/value, 8 workers | 172,178 | 163,029 | 0.947 | 1,102 | 1,165 | 1.057 | 0.725 |
| key/set, 8 workers | 139,421 | 139,845 | 1.003 | 1,421 | 1,355 | 0.954 | 0.652 |
| key/map, 8 workers | 148,385 | 158,080 | 1.065 | 1,221 | 1,257 | 1.029 | 0.756 |

Across all 33 pairs, the median candidate/baseline CPU-tick ratio is `1.006`: effectively equal CPU work. Median wall ratio is `0.969`. The candidate does not show a consistent CPU-cost increase across families.

## Pair-floor violations

| Cell | Pair | Wall ratio | CPU-tick ratio | Involuntary-switch ratio |
|---|---:|---:|---:|---:|
| key/value | 10 | 0.725 | 1.552 | 2.730 |
| key/set | 7 | 0.652 | 1.910 | 4.369 |
| key/map | 5 | 0.756 | 1.574 | 6.851 |

Each below-floor candidate sample consumed substantially more CPU ticks and suffered far more involuntary switches than its paired baseline. The same phenomenon also penalized baseline samples: when baseline CPU ticks and involuntary switches rose, several pairs reversed to favor the candidate by `1.13` to `1.44`.

Across all pairs, wall ratio versus CPU-tick ratio has Pearson correlation `-0.916`. The slow side is determined by per-sample CPU service and scheduling behavior, not consistently by implementation identity.

## Remediation recommendation

Keep SC-005's `0.90` median and `0.85` every-pair thresholds unchanged. Prospectively amend only the benchmark environment:

1. Reserve CPU 11 for the coordinator.
2. Pin worker `n` to exactly CPU `12 + n`, so eight workers occupy distinct cores `12-19`; a single worker uses CPU 12.
3. Verify every thread's effective affinity through `/proc/thread-self/status` before releasing the timed barrier.
4. Keep linked baseline/candidate code, AB/BA ordering, sample sizes, write-once outputs, and all acceptance thresholds unchanged.
5. Build and smoke-test a fresh full six-cell runner, pause for quiet-machine approval, and treat that new capture as the next SC-005 attempt.

This removes worker migration and coordinator/worker competition without modifying production code, adding unsafe code, discarding failed evidence, or weakening the gate.
